# This file may be committed to your app code.
version: alpha
# This should match the app - package name in your setup.py
name: earthobserver
setup_path: setup.py

conda:
  channels:
    - conda-forge
  dependencies:
    - netcdf4
    - numpy
    - pandas
    - rasterstats
    - rasterio
    - pygrib